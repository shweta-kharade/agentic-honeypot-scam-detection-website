version: '3.8'

services:
  agentic-honeypot:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_KEY=${API_KEY:-hackathon-submission-2024}
      - PORT=8000
    restart: unless-stopped